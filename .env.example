PORT=3000
ADMIN_PASSWORD=changeme

# Secret used to sign the admin session cookie, preventing forgery.
# Generate one with: node -e "require('crypto').randomBytes(32).toString('hex')" | pbcopy
# If omitted, a random secret is generated at startup (admin sessions reset on restart).
COOKIE_SECRET=
